# fxxk_file v1.6.2

2026-08-27

修复 Markdown 表格在宽松分隔符和窄屏场景下的渲染问题。

## 修复

- 支持 `:--`、`--:` 等常见的两短横线表格分隔符，避免表格被降级为普通段落文本。
- 支持表头与分隔行之间的单个空行、无外侧管道符，以及代码片段中的 `|`。
- 修复段落解析吞掉后续表格的问题，并对缺失单元格进行安全补齐。
- 窄屏或宽表格继续使用内容区域的横向滚动，保留列宽自适应和固定表头样式。
- 更新 Markdown 脚本资源缓存版本，确保浏览器加载最新解析器。

## 验证

- 使用 `focused_topic_chain_and_test_design.md` 验证 7 个表格均能生成独立表格 DOM。
- 覆盖短分隔符、空行、无外侧管道符和代码片段内 `|` 的自定义用例。
- 通过 JavaScript 语法检查、Python 编译检查和 `git diff --check`。

---

# fxxk_file v1.6.1

2026-08-27

修复前端功能初始化回归，并完成前后端端到端检查。

## 修复

- 修复 Markdown 模块返回未定义 `updateFullscreenButton` 导致的 JavaScript `ReferenceError`；该错误会中断 `app.js` 初始化，使翻译、对比和图片转换等功能事件无法绑定。
- 更新前端脚本和样式资源版本号，避免浏览器继续使用旧缓存。

## 验证

- 通过真实 Chromium 页面检查，无应用运行时异常。
- 验证 Markdown 打开、实时预览和表格渲染，以及翻译、双文件对比、图片转换功能切换。
- 验证文档上传、预览、段落编辑、自动保存、导出、下载准备和删除全链路。

---

# fxxk_file v1.6.0

2026-08-27

本次更新完成后端模块化重构，缩短 FastAPI 入口并补充可持续集成检查，保持现有接口路径和翻译流程兼容。

## 重构

- `app/main.py` 现在只负责导出 ASGI 应用，应用工厂、生命周期和静态资源挂载统一放入 `app/application.py`。
- 按职责拆分系统、文档和翻译 API 路由，Pydantic 请求模型集中到 `app/api/schemas.py`。
- 将 DOCX/PDF 预览 HTML 移入 `app/services/preview.py`，将后台翻译批处理、重试和进度逻辑移入 `app/services/translation_job.py`。
- 使用 `app/core/runtime.py` 集中管理上传大小限制和运行中任务，避免路由循环导入入口文件。

## CI/CD

- 新增 `.github/workflows/ci.yml`，在 push 和 pull request 时自动安装依赖、编译 Python、导入应用并检查关键路由。
- 新增 [后端结构说明](docs/BACKEND_STRUCTURE.md)，明确路由、服务和运行时状态的模块边界及本地检查命令。

## 验证

- 通过 `python -m compileall -q app tools`。
- 通过应用导入、路由枚举和首页、健康检查、设置及静态资源 HTTP smoke test。
- 通过 `git diff --check`。

---

# fxxk_file v1.5.0

2026-08-27

本次更新完善 Markdown 工作区，并按页面和功能重构前端静态资源，方便后续维护与 CI/CD。

## 新增

- Markdown 支持源码编辑与实时预览，编辑器和预览区可同步滚动。
- 支持下载当前编辑后的 Markdown 文件，编辑模式下可使用 `Ctrl + S` 快速下载。
- 对比、图片转换和 Markdown 页面逻辑分别拆分为独立功能模块。

## 重构

- 将混合在单个文件中的 CSS 拆分为 tokens、基础控件、布局、翻译、对比、图片、Markdown 和响应式样式模块。
- `styles.css` 保留为兼容入口，仅负责按固定顺序导入各 CSS 模块。
- 新增前端结构说明和 CI/CD 基础检查约定，页面资源版本号统一更新，降低部署缓存风险。
- 保持现有 FastAPI 静态资源路径和无构建步骤启动方式不变。

## 验证

- 已通过所有前端 JavaScript 模块语法检查。
- 已通过 Python 模块编译检查和 `git diff --check`。
- 已通过本地 FastAPI 静态资源访问检查。

---

# fxxk_file v1.4.0

2026-08-27

本次更新新增独立的 Markdown 查看器功能页面，方便阅读长篇 Markdown 文档。

## 新增

- 新增“Markdown 查看”功能入口，可从顶部“功能”菜单直接切换。
- 支持打开 `.md`、`.markdown`、`.mdown`、`.mkdn` 和 `.txt` 文件，也支持拖拽导入。
- Markdown 文件在浏览器本地读取和渲染，不上传到翻译服务或应用服务器。
- 支持标题、目录、段落、强调、删除线、链接、图片、引用、列表、任务列表、分隔线和代码块等常用 Markdown 语法。
- 自动生成文档目录，点击目录项可平滑跳转到对应标题。
- 新增全屏查看模式，并提供退出全屏和 Esc 退出支持。
- 支持阅读字号调整，适应不同屏幕尺寸和阅读距离。
- 表格自动识别行数与列数；根据可用宽度自动换行、压缩单元格间距或启用横向滚动，避免宽表格撑破页面。
- Markdown 页面在窄屏设备上自动隐藏目录并切换为单栏布局，提升移动端阅读体验。

## 验证

- 使用 `find_topic_analysis.md` 进行长文档验证：593 行、28 个标题、3 个表格，其中最大表格包含 163 行、7 列，均可正常解析和渲染。
- 已通过 JavaScript 语法检查、Python 模块编译检查和本地 FastAPI 静态页面访问检查。

---

# fxxk_file v1.3.0

本次更新新增「图片转换」工作区，提供 PNG 转 SVG 与 SVG 转 PNG 双向转换，全程在浏览器本地完成，图片不会上传到服务器。

## 新增

- 新增「图片转换」功能：通过顶部「功能」菜单切换到「图片转换」，内置 PNG→SVG 与 SVG→PNG 两个方向，可随时切换。
- PNG→SVG 矢量化：自动量化颜色、清理噪点并追踪轮廓，支持色块轮廓、线段中心线、内轮廓填充三种生成方式；可调整颜色数量、轮廓简化、追踪精度、忽略小色块、相似色合并、背景移除等参数；支持输出色板改色、SVG 源码查看、复制与下载。
- SVG→PNG 光栅化：支持拖入 SVG 文件或直接粘贴 SVG 代码，按倍数或宽高导出高清 PNG；可设置透明、白色或自定义背景，支持下载与复制到剪贴板；自动处理 cm、pt 等单位以及缺少尺寸声明的 SVG。
- 图片模式下可把文件直接拖进内嵌工具使用，拖放不会误触发文档翻译上传。
- 切换「功能」时，左侧栏会同步切换对应内容：翻译模式显示拖入与最近文档，双文件对比和图片转换模式显示各自的操作提示。

## 文档

- README 补充「图片转换」功能说明。

## 升级说明

- 从 `v1.2.0` 升级无需迁移数据或修改现有配置。更新代码后按原方式运行 `翻译脚本.cmd` 或 `start.ps1` 即可。

## 已知限制

- 旧版 `.doc` 文件仍需要 LibreOffice 转换；扫描版 PDF 需要先添加 OCR 文本层。
- SVG 转 PNG 依赖浏览器渲染，含外部资源或特殊字体的 SVG 可能无法正确导出，建议使用自包含 SVG。

**完整变更范围：** `v1.2.0..v1.3.0`

---

# fxxk_file v1.2.0

本次发布正式将项目更名为 fxxk_file，并新增「双文件对比」工作区，支持文档与文档、图片与图片的并排对照。

## 新增

- 新增「双文件对比」功能：通过顶部「功能」菜单切换到「双文件对比」，可并排打开两份文件。
- 支持文档 + 文档对比（DOC、DOCX、PDF），左右按页同步滚动，缩放范围 50%–300%。
- 支持图片 + 图片对比，左右共享缩放，按住鼠标右键拖拽可平移图片。
- 对比模式支持拖拽导入：在对比模式下把两份同类型文件拖进窗口即可自动载入；混合类型（文档 + 图片）会被拒绝并提示。
- 对比模式与翻译模式使用独立配色主题，切换时有平滑过渡。

## 改进

- 文本分段更智能：新增中英文换行拼接逻辑。中文跨行拼接不再插入多余空格（例如“组合驾驶\n辅助系统”合并为“组合驾驶辅助系统”），英文跨行则保留一个空格，减少 PDF 提取和长段落拆分时产生的错误断词。
- 下拉框、按钮、功能菜单等控件样式统一升级（渐变、悬停、焦点态），提升操作反馈与一致性。
- 原有逐段对照、原件预览等视图与主题保持兼容。

## 文档

- 重写 README，补充双文件对比、目录结构、配置项与安全说明。
- 使用手册新增「双文件对比」章节，并完善界面说明与故障排查。
- 架构文档补充双文件对比工作区的实现说明。

## 升级说明

- 本次发布将项目正式更名为 fxxk_file（原 Chanslator / Translator / LinguaBridge）。
- 从 `v1.1.0` 升级无需迁移数据或修改现有配置。更新代码后按原方式运行 `翻译脚本.cmd` 或 `start.ps1` 即可。

## 已知限制

- 旧版 `.doc` 文件仍需要 LibreOffice 转换；扫描版 PDF 需要先添加 OCR 文本层。
- PDF 译文输出为新的 DOCX，不保留原 PDF 版式。
- 图片内文字、文本框、批注、SmartArt、嵌入对象和复杂域等内容仍需人工复核。
- 双文件对比为只读对照，暂不支持在对比视图中直接编辑文档内容。

**完整变更范围：** `v1.1.0..v1.2.0`

---

# fxxk_file v1.1.0

本次更新重点改善文档对照审校体验，并增强编辑内容的保存可靠性。

## 亮点

- 新增原文预览缩放，支持在 60%–200% 之间调整，DOCX 与 PDF 均可使用。
- 新增可拖动的左右分栏，可自由调整原文与译文区域宽度；双击分隔条可恢复均分。
- 新增译文字号调节，支持 14–22 px，便于长时间审校和不同屏幕尺寸使用。
- 显示偏好会保存在当前浏览器中，下次打开时自动恢复。

## 改进

- 优化自动保存流程，按文档和段落跟踪等待中及进行中的保存请求，减少快速编辑、切换文档或关闭窗口时内容未写入的风险。
- 标记“已审校”前会等待当前段落保存完成，避免操作顺序导致状态或译文被旧请求覆盖。
- DOCX/PDF 缩放时尽量保持当前阅读位置，并重新计算同步翻页位置。
- 优化窄窗口和不同分栏宽度下的工具栏、标题栏及审校区域布局。
- 逐段对照视图采用固定标题栏，浏览长文档时更容易确认原文与译文列。
- 完善按钮禁用、保存失败提示和无障碍键盘操作。

## 文档

- 新增完整的中文使用手册，覆盖启动、模型配置、文档上传、翻译审校、快捷键、术语表、导出、安全说明及故障排查。
- 重写 README，补充支持格式、接口协议、目录结构、环境变量、数据安全和常见问题说明。

## 升级说明

从 `v1.0.0` 升级无需迁移数据或修改现有配置。更新代码后按原方式运行 `翻译脚本.cmd` 或 `start.ps1` 即可。

## 已知限制

- 旧版 `.doc` 文件仍需要 LibreOffice 转换；扫描版 PDF 需要先添加 OCR 文本层。
- PDF 译文输出为新的 DOCX，不保留原 PDF 版式。
- 图片内文字、文本框、批注、SmartArt、嵌入对象和复杂域等内容仍需人工复核。
- 工作区任务记录会在程序正常退出时清理，但原件和已生成译文会继续保留。

**完整变更范围：** `1.0.0..v1.1.0`
